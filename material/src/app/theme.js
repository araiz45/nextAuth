"use client";
import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#a11b67",
      light: "#56bd37",
    },
    secondary: {
      main: "#997c25",
    },
    otherColor: {
      main: "#2d5c1e",
      light: "#2b2369",
    },
  },
});
